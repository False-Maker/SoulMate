# 🚀 SoulMate 项目启动手册 (Getting Started Guide)

欢迎来到 SoulMate 灵伴开发社区！本文档将引导你完成开发环境的搭建、配置注入以及项目的首次运行。

---

## 📋 1. 环境准备 (Prerequisites)

在开始之前，请确保你的开发环境满足以下要求：

### 硬件要求
*   **操作系统**: Windows 10/11, macOS, 或 Linux
*   **内存**: 建议 16GB+ (Android Studio + Gradle 编译开销)
*   **测试设备**: **强烈建议使用真机** (Android 8.0+, API 26+)。
    *   *注：由于项目涉及大量的麦克风 (ASR) 和传感器操作，模拟器可能无法正常体验语音唤醒与打断功能。*

### 软件要求
*   **IDE**: [Android Studio Koala](https://developer.android.com/studio) 或更新版本 (推荐 Ladybug Feature Drop)。
*   **JDK**: JDK 17 (项目默认编译配置)。
*   **Android SDK**:
    *   Compile SDK: 34 (Android 14)
    *   Min SDK: 24 (Android 7.0)

---

## 🔑 2. 关键配置 (Configuration)

SoulMate 依赖多个云端服务（豆包大模型、阿里云 ASR、魔珐数字人）。为了保护敏感信息，我们使用 `local.properties` 进行密钥管理。

### 步骤 2.1: 创建/配置 local.properties

在项目根目录下找到或创建 `local.properties` 文件，并添加以下配置：

```properties
## Project-wide configuration
sdk.dir=C\:\\Users\\YourName\\AppData\\Local\\Android\\Sdk

## ==========================================
## 🧠 灵核配置 (Soul Core - LLM)
## ==========================================
# 申请地址: https://console.volcengine.com/ark
DOUBAO_API_KEY="sk-xxxxxxxxxxxxxxxxxxxxxxxx"
DOUBAO_MODEL_ID="ep-202xxxxxxxx-xxxxx"

## ==========================================
## ✨ 灵犀化身 (Soul Avatar - Xmov/Others)
## ==========================================
# 申请地址: (魔珐科技或对应的数字人服务商)
# 如果暂时没有 key，应用会降级运行，但数字人画面可能无法加载
XMOV_APP_ID="xm_xxxxxx"
XMOV_APP_SECRET="xxxxxxxxxxxx"

## ==========================================
## 👂 听觉神经 (Hearing - Aliyun ASR)
## ==========================================
# 申请地址: https://nls-portal.console.aliyun.com/
ALIYUN_ASR_APP_KEY="xxxxxxxx"
ALIYUN_ACCESS_KEY_ID="LTAIxxxxxxxx"
ALIYUN_ACCESS_KEY_SECRET="xxxxxxxxxxxxxxxx"
```

> ⚠️ **注意**: 请勿将包含真实 Key 的 `local.properties` 提交到版本控制系统！

---

## 🛠️ 3. 编译与运行 (Build & Run)

### 步骤 3.1: 同步 Gradle
打开 Android Studio，点击工具栏的 **Sync Project with Gradle Files** (大象图标)。
*   等待依赖下载完成 (首次可能需要几分钟)。
*   确保没有报错 (Build Successful)。

### 步骤 3.2: 运行应用
1.  连接 Android 真机，并开启 **USB 调试** 模式。
2.  在 Android Studio 顶部工具栏选择 `app` 模块。
3.  点击 **Run** (绿色三角形)。

---

## 🧩 4. 常见问题 (Troubleshooting)

### Q1: 启动时报错 `240007` (ASR 初始化失败)
*   **原因**: ASR SDK 初始化时网络连接不稳定或 Token 获取失败。
*   **解决**:
    *   检查设备网络是否正常。
    *   确认 `local.properties` 中的阿里云 Key 是否有效且账户有余额。
    *   v1.3 版本已增加自动重试机制，请稍等片刻或重启 App 尝试。

### Q2: 模拟器无法录音或无声音
*   **原因**: 模拟器对音频通道的支持有限，且不支持回声消除 (AEC)。
*   **解决**: 请务必使用 **真机** 进行调试。

### Q3: 找不到 Hilt 生成的类 (DaggerApplication 等)
*   **原因**: KAPT/KSP 缓存问题。
*   **解决**: 执行 `Build` -> `Clean Project`，然后 `Rebuild Project`。

---

## 📚 5. 架构概览

如果你想深入代码，建议从以下路径开始：

*   **UI层**: `ui/screens/DigitalHumanScreen.kt` (主交互页)
*   **业务层**: `ui/viewmodel/ChatViewModel.kt` (核心状态机)
*   **数据层**: `data/service/` (MindWatch, ASR, AvatarService)

---

祝你在 SoulMate 的世界里探索愉快！🎉
